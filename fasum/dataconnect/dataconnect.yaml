specVersion: "v1"
serviceId: "fasum"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "fasum-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
